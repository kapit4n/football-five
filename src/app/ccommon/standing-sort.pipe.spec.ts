import { StandingSortPipe } from './standing-sort.pipe';

describe('StandingSortPipe', () => {
  it('create an instance', () => {
    const pipe = new StandingSortPipe();
    expect(pipe).toBeTruthy();
  });
});
